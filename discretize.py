def find_best_threshold(theta, observations, feature, priors, G, hypotheses):
    """
    returns a threshold to binarize the feature. 
    parameters:
        theta: ndarray containing the current estimate for the parameters.
        observations: dictionary containing the observed features as keys and observed values.
        feature: the feature to binarize
        priors: the prior probs for decision regions.
        G: the graph
        hypotheses: the remaining hypotheses
    """
    pass


